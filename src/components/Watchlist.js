import React from 'react'

// ternary in card just like in program.js except the truthy statement will be watchlist == false ? null : render card element
//you should add a sort method here also. Such as sort alphabetically by title or network
const Watchlist = () => {
  return(
    <div>Watchlist</div>
  )
}

export default Watchlist
